#include "../includes/push_swap.h"

void ft_sort()
{
  int i;
  int j;
  define struct stack_a {
    int sa;
    int sb;
    int ss;
    int ra;
    int rb;
    int rr;
    int pa;
    int pb;
    int rra;
    int rrb;
    int rrr;
    int next;
  } stack_a;
}

void ft_myAlgorithm(int z)
{
  if (stack_a->next.node == NULL)
    return (NULL);
  else
    free(stack_a->next.node)
}

int main(int ac, char *av[])
{
  size_t i = {0};

  if (ac < 2)
    perror("not enough arguments");
    return (0);
  while(av[1][i] >= 0)
  {
    ft_sort(&av[i++]);
    sorted = (char *)malloc(sizeof(av[i]));
    if (sorted == NULL)
    {
      free(sorted);
      return (NULL);
    }
    i++;
  }
  ft_myAlgorithm(av[i]);
  break ;
}
